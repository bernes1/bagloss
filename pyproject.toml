[project]
name = "bagloss"
version = "0.1.0"
description = "A simple service that sends a push nofication and tells you that github is down"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
